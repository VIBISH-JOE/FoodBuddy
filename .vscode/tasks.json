{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run two commands",
            "type": "shell",
            "command": "npm run build; npm start",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}